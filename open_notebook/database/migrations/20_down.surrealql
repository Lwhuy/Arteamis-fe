-- Migration 20 rollback: remove visibility field, drop membership first (references workspace), then workspace.
REMOVE FIELD visibility ON source;
REMOVE TABLE IF EXISTS membership;
REMOVE TABLE IF EXISTS workspace;
